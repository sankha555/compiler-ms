#include "keywordMap.h"
#include <stdio.h>
#include <stdlib.h>

#define BUFFER_SIZE 4096    // buffer size in bytes

char** init_buffer(FILE* fp){
    char buffer[2*BUFFER_SIZE]; 

    return buffer;
}